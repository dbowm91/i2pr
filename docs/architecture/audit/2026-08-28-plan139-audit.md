# Architecture documentation update — 2026-08-28

Plan 139 closed the SAM 3.1 local forward/naming hardening lane. The
current architecture and support references were reconciled with the source:

- `i2pr-api` now documents `forward.rs`, `naming.rs`, typed forward/naming
  dispatch outcomes, and atomic `InboundMode` ownership.
- `i2pr-daemon` now documents the SAM composition root, ownership-bound
  loopback forwarding, bounded cancellation-aware raw bridging, and local
  naming resolution.
- The repository status references now point to Plan 139 as the current SAM
  closure authority and Plan 140 as the next executable interoperability
  plan.
- The protocol-support ledger records Plan 139 separately as experimental
  and non-advertised. Historical Plan 136–138 rows remain unchanged as
  closure records rather than being rewritten retroactively.

The remaining same-socket raw CONNECT/ACCEPT handoff and independent-router
SAM interoperability are intentionally retained as Plan 140 scope.
