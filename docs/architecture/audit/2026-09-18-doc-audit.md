# Architecture Documentation Audit — 2026-09-18

Systematic parallel-subagent pass over every file in
`docs/architecture/` against the source tree on `main`. Five
`explore` subagents were dispatched in parallel (foundation,
transport+runtime, netdb+tunnel, client/api/service, daemon/tooling),
each research-only; the main agent verified every high-impact claim
against source before patching.

## Patches applied (same commit)

| Doc | Fix |
| --- | --- |
| `overview.md` §4.2 | Narrowed `i2pr-crypto` scope: Ed25519/X25519/SHA-256/HKDF/Elligator2/ECIES only. Transport ciphers (ChaCha20-Poly1305, AES-CBC, HMAC, SipHash, Noise) live in `i2pr-transport-ntcp2`. Previously contradicted `i2pr-crypto.md:51-58`. |
| `overview.md` §4.11 | Added `zero_hop` (Plan 172 loopback delivery) to the tunnel summary. |
| `i2pr-proto.md` | Module layout: two flat files → three (`codec.rs`, `ecies_payload.rs`, `i2cp_data_body.rs`); added the `i2cp_data_body.rs` row (Plan 192 inbound-delivery codec, confirmed in `crates/i2pr-proto/src/lib.rs:13,20`). Deps: added `flate2` (confirmed in `crates/i2pr-proto/Cargo.toml`). |
| `i2pr-crypto.md` | `EciesError`: 13 variants + `Crypto` forwarding → 12 variants, no `Crypto` variant (counted in `crates/i2pr-crypto/src/ecies.rs:107-166`; `Crypto` lives on `CryptoError`). Wrapper text: `curve25519-elligator2` → `elligator2` (Plan 131 switch). Design choice: "Two-file" → "Three-file" (`lib.rs`/`hkdf.rs`/`ecies.rs`). |
| `i2pr-storage.md` | `StorageError`: removed non-existent `Cache` variant; `CacheError` converts via `From` into `StorageError::Io` (`crates/i2pr-storage/src/lib.rs:78-137,494-509`). Module layout: surfaced `src/service_destination.rs` (`pub mod service_destination`, 895 lines). |
| `i2pr-runtime.md` | `ntcp2_data_oracle.rs`: 13.9 K → 359 lines; `context.rs`: 585 → 692; `ssu2_runtime.rs`: 5094 → 5198; `ssu2_peer_relay.rs`: added 1182 lines (all via `wc -l`). |
| `i2pr-transport.md` | `AddressObservation` anchor: `lib.rs:46` → `lib.rs:57` (confirmed in `crates/i2pr-transport/src/lib.rs:57`). |
| `i2pr-tunnel.md` | Module table: added missing `zero_hop` row (Plan 172, `crates/i2pr-tunnel/src/lib.rs:89`, 264 lines); clarified "22 modules plus the crate root". |
| `i2pr-client.md` | Layout tree: `streaming.rs` single file → `streaming/` directory (14 files); tests tree: added `plan132_trajectory.rs` + `plan166_trajectory.rs` (both exist on disk; `plan133` has no test file and was not added). Text: `curve25519-elligator2` → `elligator2`. |
| `i2pr-api.md` | Plan 165 action list: noted `RequestVariableLeaseSet` (Plan 166, `crates/i2pr-api/src/i2cp/actions.rs:34+`) lives below; `StreamRawMode`: standalone type → variant of `DispatchOutcome` (`crates/i2pr-api/src/sam/server_state.rs:52,107`). |
| `i2pr-daemon.md` | Module layout: "twenty-three files, no subdirectories" → 25 files at root + 4 in `src/sam/`; added missing rows (`destination_streaming`, `service_generation`, `service_product`, `service_tunnels`, `service_tunnels_http/socks5/irc_client/irc_server`, `sam/faults`, `sam/raw_stream`); removed the stray `tests/integration/...` row. |
| `tooling.md` | Scripts table: added the four missing evidence checkers present on disk and referenced from `overview.md` (`check-exploratory-tunnel-evidence.sh`, `check-netdb-tunnel-evidence.sh`, `check-destination-tunnel-evidence.sh`, `check-streaming-tunnel-evidence.sh`). `check-m6-final-closure-evidence.sh` was already present. |

## Subagent finding rejected after verification

- `i2pr-client.md` "phantom `plan132_trajectory.rs`" — rejected:
  `crates/i2pr-client/tests/plan132_trajectory.rs` exists on disk, so
  the doc tree was stale by omission, not by phantom reference. Fixed
  by adding the row rather than deleting the prose.

## Outstanding gaps (not patched)

1. Stale `lib.rs` line-number anchors remain across several deep
   dives (proto, crypto, storage, transport). Line numbers drift on
   every edit; the tables are correct on names/types but not on lines.
   Consider dropping exact line anchors or regenerating them
   mechanically.
2. `i2pr-transport-ntcp2.md` module `~` line-count approximations
   drift 2–8%; within tolerance, left as-is.
3. `i2pr-testkit.md`, `dependency-graph.md`,
   `interop-apparatus.md` verified GOOD — no changes.
4. `fuzz/fuzz_targets/` count: 23 files on disk vs "22 targets" in
   prose; `support.rs` is a shared module, not a target, so the prose
   count stands. Left as-is.
5. Plan-history narratives in the deep dives were not re-verified
   against `plans/*-status.md` tokens beyond the cited Plans
   101/131/155-172/184-215; authority remains with the status files.
