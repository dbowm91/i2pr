# Plan 206 status — M10 production remote delivery composition corrective

Status: **`registered-executable-m10-production-remote-delivery-corrective`**.

Plan of record: [`206-m10-production-remote-delivery-composition-corrective.md`](206-m10-production-remote-delivery-composition-corrective.md).

Plan 206 corrects the proof gap left by Plan 202. The retained Plan 202 work is useful but partial: `RoutingDecision`, `ServiceDestinationDelivery`, bounded counters, manager installation APIs, and external lower-stack interop scaffolding exist. What is not yet proven is that a normal `ServiceTunnelManager` connection actually routes its own Streaming requests through the real remote LeaseSet2/tunnel/router backend, or that inbound remote traffic is delivered to the owning M10 service runtime.

Current authority:

```text
plan_202 = partial-m10-remote-routing-capability-surface-superseded-by-plan206
plan_203 = partial-m10-remote-application-evidence-scaffolding-superseded-by-plan207
plan_206 = registered-executable-m10-production-remote-delivery-corrective
plan_207 = registered-blocked-by-plan206
m10_remote_transport_core = not-yet-passed
m10_remote_application_interop = not-yet-passed
m10_remote_rows = non-authoritative / must be re-proven
```

Execution graph:

```text
plan205 || plan206 may execute in parallel
plan206 -> plan207
plan205 + plan207 -> plan204 convergence
```

Plan 206 must prove actual generic-client and generic-server product I/O beginning at the manager's real listeners. A pass may not be derived from `routing_decision_for()` plus a separately constructed Plan 184–193 stack. Remote success counters must be generated by production operations rather than manually advanced in the acceptance driver.

On success, set:

```text
plan_206 = passed-m10-production-remote-delivery-composition-corrective
m10_remote_transport_core = passed-via-plan206
next_m10_application_plan = 207
```
