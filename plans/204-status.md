# Plan 204 status — final M6/M10 evidence and authority convergence

Status: **`blocked-on-plan205-java-and-plan206-207-m10-correctives`**.

Plan of record: [`204-m10-final-closure-evidence-authority-and-documentation-normalization.md`](204-m10-final-closure-evidence-authority-and-documentation-normalization.md).

This status supersedes the earlier interpretation that Plan 204 was blocked only on the Java Plan 205 branch. A source-level audit of the landed Plan 202/203 work found that the M10 remote rows were promoted beyond the evidence actually produced.

Plan 204 remains a convergence/authority pass. It must not hide product or evidence defects. It therefore waits for both independent branches:

```text
Java branch:
  plan205 -> close mandatory Java M6 second-family rows

M10 branch:
  plan206 -> real ServiceTunnelManager remote delivery composition
  plan207 -> genuine system curl + exact-pinned jaraco/irc remote application evidence

convergence:
  plan205 + plan207 -> rerun plan204 exact-head closure
```

## Corrected authority

```text
plan_200 = retained diagnostic/evidence pass
plan_201 = in-progress Java corrective history
plan_202 = partial-m10-remote-routing-capability-surface-superseded-by-plan206
plan_203 = partial-m10-remote-application-evidence-scaffolding-superseded-by-plan207
plan_204 = blocked-on-plan205-java-and-plan206-207-m10-correctives
plan_205 = registered Java SAM/helper publication pivot
plan_206 = registered-executable-m10-production-remote-delivery-corrective
plan_207 = registered-blocked-by-plan206

milestone6_java_mixed_router_interop = not-yet-passed
milestone6_interoperable = not-yet-claimed
m10_remote_transport_core = not-yet-passed
m10_remote_application_interop = not-yet-passed
milestone10_remote_service_interop = not-yet-passed
milestone10_final_acceptance = not-yet-closed
```

The three previously promoted M10 remote rows are non-authoritative until re-proven:

```text
m10-remote-destination-streaming-composition
remote-independent-http-eepsite
remote-independent-irc-service
```

## Why Plan 204 is re-blocked on M10

Plan 202's manager seam can classify a peer as `RemoteRouter`, but the counted external test constructs the real destination/tunnel/Streaming stack separately rather than proving the normal manager delivery loop invokes it. Plan 203 then advances application observation labels and emits terminal HTTP/IRC facts without binding the aggregate rows to actual system curl and jaraco/irc commands through that product path.

Those are useful scaffolding results, not final M10 acceptance.

## Final convergence requirements

Plan 204 may close only after all of the following are true on one exact head:

1. Plan 205 closes the mandatory Java M6 second-family lane.
2. Plan 206 proves real generic-client and generic-server remote product delivery through `ServiceTunnelManager`.
3. Plan 207 proves actual system curl HTTP traffic through the production remote path.
4. Plan 207 proves exact-pinned jaraco/irc traffic through the production remote path.
5. The retained 29 local M10 rows remain passed.
6. The full remote lane has zero mandatory blocked/failed/missing rows.
7. The M6 cross-family final evidence gate is green.
8. Routine CI/static/dependency/docs floors are green.
9. No authority file claims a stronger status than the same-head evidence.

Only then may Plan 204 record:

```text
milestone6_interoperable = passed-via-retained-i2pd-and-closed-java-second-family
m10_remote_transport_core = passed-via-plan206
milestone10_remote_service_interop = passed-via-plan206-and-plan207
milestone10_final_acceptance = closed-via-plan204
next_product_layer = milestone11-planning
```

Until then, final closure is prohibited.
