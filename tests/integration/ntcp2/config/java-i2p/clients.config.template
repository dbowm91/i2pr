# Plan 038 stub client configuration.
# Plan 045 references use a separate per-direction trigger that does not
# rely on the embedded SAM/HTTP client. The router must still start
# cleanly without an active client tunnel, so leave the file empty of
# clientApp entries (I2P's migrate path NPEs on the legacy stub above).
